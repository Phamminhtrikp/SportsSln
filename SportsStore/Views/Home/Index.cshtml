@model SportsStore.Models.ViewModels.ProductListViewModel
@{
	ViewBag.Title = "SportsStore - List Products";
}
@foreach (var p in Model?.Products ??
		Enumerable.Empty<SportsStore.Models.Product>())
{
	@* <div>
		<h3>@p.name</h3>
		@p.description
		<h4>@p.price.ToString("c")</h4>
	</div> *@

	<partial name="ProductSummary" model="p" />
}

@* <div page-model="@Model?.PagingInfo" page-action="Index"></div> *@
<div page-model="@Model?.PagingInfo" page-action="Index" page-classes-enabled="true"
	page-class="btn" page-class-normal="btn-outine-dark"
	page-class-selected="btn-primary" 
	page-url-category="@Model?.CurrrentCategory!"
	class="btn-group pull-right"></div>